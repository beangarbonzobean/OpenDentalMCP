[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live: requires live OD database, file share, or network (skipped by default)
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
